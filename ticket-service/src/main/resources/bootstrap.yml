spring:
  application:
    name: ticket-service
  profiles:
    active: dev
  cloud:
    config:
      uri: http://localhost:8888
      fail-fast: true
      label: ticket-service-branch

#logging:
#  level:
#    org:
#      springframework:
#        security: debug