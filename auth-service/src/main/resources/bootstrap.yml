spring:
  application:
    name: auth-service
  profiles:
    active: dev
  cloud:
    config:
      uri: http://localhost:8888
      fail-fast: true
#      label: auth-service-branch

#logging:
#  level:
#    org:
#      springframework:
#        security: DEBUG
