spring:
  application:
    name: user-service
  profiles:
    active: dev
  cloud:
    config:
      uri: http://localhost:8888
      fail-fast: true
      label: user-service-branch-impl

#logging:
#  level:
#    org:
#      springframework:
#        security: debug